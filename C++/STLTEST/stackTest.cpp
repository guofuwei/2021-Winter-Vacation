#include <iostream>
#include <stack>

using namespace std;

int main()
{
    stack<int> test;
    test.push(1);
    test.top();
    test.pop();
    test.empty();
    string s = "Hello";
    system("pause");
    return 0;
}